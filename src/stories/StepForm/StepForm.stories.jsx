import React from 'react';
import StepForm from './StepForm';

export default {
    title: 'StepForm/StepForm',
    component: StepForm
};

const Template = (args) => <StepForm {...args} />;

export const Main = Template.bind({});